```sql
--상시업데이트
--ORACLE
-- TRUNC() : TRUNC 함수는 주로 소수점 절사 및 날짜의 시간을 없앨 때 사용한다.
-- 사용법   : TRUNC(컬럼명)

--TO_CHAR() : 문자열로 바꾸기
--예시) SELECT TO_CHAR(123456,'999,999') FROM DUAL; //결과값 : 123,456

--TO_DATE() : 문자열을 날짜형데이터로 바꾸는 함수
-- 예시) SELECT TO_DATE('20201230', 'YYYY-MM-DD') FROM dual;



```